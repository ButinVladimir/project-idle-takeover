import { css } from 'lit';
import { itemsListButtonsStyle } from '@shared/index';

const styles = [
  itemsListButtonsStyle,
  css`
    :host {
      display: contents;
    }
  `,
];

export default styles;
