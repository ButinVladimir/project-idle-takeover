{
  "type": "object",
  "properties": {
    "$schema": {
      "type": "string"
    }
  },
  "additionalProperties": {
    "type": "object",
    "properties": {
      "activityRequirementModifier": {
        "type": "number",
        "description": "District type activity requirement modifier"
      },
      "primaryActivityTimeMultipliers": {
        "type": "object",
        "description": "District type primary action time multipliers",
        "properties": {
          "completionTime": {
            "type": "number",
            "description": "Primary action completion time multiplier"
          },
          "generationTime": {
            "type": "number",
            "description": "Primary action generation time multiplier"
          }
        }
      },
      "synchronization": {
        "$ref": "./common/geometric-progression.json",
        "description": "District type synchronization modifier (geometric)"
      },
      "parameters": {
        "type": "object",
        "description": "District type parameters",
        "properties": {
          "experience": {
            "$ref": "#/$defs/multiplierParameter",
            "description": "District type experience modifier (geometric)"
          },
          "money": {
            "$ref": "#/$defs/multiplierParameter",
            "description": "District type money modifier (geometric)"
          },
          "developmentPoints": {
            "$ref": "#/$defs/multiplierParameter",
            "description": "District type development points modifier (geometric)"
          },
          "influence": {
            "$ref": "#/$defs/multiplierParameter",
            "description": "District type faction influence modifier (geometric)"
          },
          "connectivity": {
            "$ref": "#/$defs/multiplierParameter",
            "description": "District type connectivity modifier (geometric)"
          },
          "codeBase": {
            "$ref": "#/$defs/multiplierParameter",
            "description": "District type code base modifier (geometric)"
          },
          "computationalBase": {
            "$ref": "#/$defs/multiplierParameter",
            "description": "District type computational base modifier (geometric)"
          },
          "rewards": {
            "$ref": "#/$defs/multiplierParameter",
            "description": "District type rewards modifier (geometric)"
          },
          "processCompletionSpeed": {
            "$ref": "#/$defs/multiplierParameter",
            "description": "District type process completion speed modifier (geometric)"
          },
          "experienceShareMultiplier": {
            "$ref": "#/$defs/multiplierParameter",
            "description": "District type experience share multiplier modifier (geometric)"
          }
        },
        "required": [
          "experience",
          "money",
          "developmentPoints",
          "influence",
          "connectivity",
          "codeBase",
          "computationalBase",
          "rewards",
          "processCompletionSpeed",
          "experienceShareMultiplier"
        ]
      },
      "requirements": {
        "type": "object",
        "description": "District type requirements",
        "properties": {
          "influence": {
            "$ref": "./common/geometric-progression.json",
            "description": "District type faction influence requirements for the next tier (geometric)"
          }
        },
        "required": ["influence"]
      },
      "multiplierParameterBases": {
        "type": "object",
        "description": "District type multiplier parameters bases",
        "properties": {
          "codeBase": {
            "$ref": "#/$defs/multiplierParameterBase",
            "description": "District type code base value"
          },
          "computationalBase": {
            "$ref": "#/$defs/multiplierParameterBase",
            "description": "District type computational base value"
          },
          "rewards": {
            "$ref": "#/$defs/multiplierParameterBase",
            "description": "District type rewards value"
          }
        },
        "required": ["codeBase", "computationalBase", "rewards"]
      }
    },
    "required": [
      "activityRequirementModifier",
      "synchronization",
      "parameters",
      "requirements",
      "multiplierParameterBases"
    ]
  },
  "$defs": {
    "multiplierParameter": {
      "type": "object",
      "properties": {
        "progression": {
          "$ref": "./common/geometric-progression.json",
          "description": "Parameter progression (geometric)"
        },
        "exponent": {
          "type": "number",
          "description": "Parameter exponent"
        }
      },
      "required": ["progression", "exponent"]
    },
    "multiplierParameterBase": {
      "type": "number",
      "description": "Logarithmic base"
    }
  }
}
