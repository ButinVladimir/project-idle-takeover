import { css } from 'lit';

const styles = [
  css`
    :host {
      display: block;
    }

    p.rewards {
      margin-top: var(--sl-spacing-small);
      margin-bottom: 0;
    }

    p.text {
      margin: 0;
    }
  `,
];

export default styles;
